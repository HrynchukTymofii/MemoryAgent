# Signed + notarised macOS build for distribution

- `scripts/build-macos.sh` — new: one command, checks credentials, builds, notarises
- `.env.example` — Apple signing/notarisation vars
- `README.md` — what to get from Apple, in order
